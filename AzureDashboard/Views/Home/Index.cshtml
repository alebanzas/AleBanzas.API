@model ABServicios.Azure.Storage.DataAccess.TableStorage.Queries.VotacionModel
@using Microsoft.Owin.Security
@{
    ViewBag.Title = "Resultados";  
    Random rnd = new Random();
}


<div class="resultados fondo">
    <div class="row">
        <div class="cell title"></div>
        <div class="cell title"><p>visitas</p></div>
        <div class="cell title"><p>amigos</p></div>
        <div class="cell title"><p><b>total</b></p></div>
        <div class="cell title"><p>puntos</p></div>
    </div>
    @{
        for (int index = 1; index <= Model.Lista.Count; index++)
        {
            var item = Model.Lista[index - 1];
            <div class="row">
                <div class="cell"><p>#@(index)&nbsp;&nbsp;&nbsp;&nbsp;<a href="http://@item.Nombre" style="color: white;">@item.Nombre</a></p></div>
                <div class="cell"><p class="tile-@(rnd.Next(1, 5))">@(item.Visitas)</p></div>
                <div class="cell"><p class="tile-@(rnd.Next(1, 5))">@(item.VisitasReferidas)</p></div>
                <div class="cell"><p class="tile-@(rnd.Next(1, 5))"><b>@(item.Visitas + item.VisitasReferidas)</b></p></div>
                <div class="cell"><p class="tile-@(rnd.Next(1, 5))">@(item.Puntos)</p></div>
            </div>
        }
}

    <p style="font-size:10px; display: none;">
        <br />
        Este resultado es parcial, el sistema cierra la votacion a las 0hs del 00/00/2014. Cuando el resultado sea final, publicaremos por este medio los ganadores finales.
        <br />
        El resultado esta auditado por accesos duplicados desde una misma IP el mismo dia.
    </p>
</div>
